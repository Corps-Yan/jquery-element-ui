<div class="el-autocomplete-suggestion">
    <div class="el-autocomplete-loading">
        <i class="el-icon-loading"></i>
    </div>
    <% if(dataList) { %>
        <ul class="el-autocomplete-suggestion-data">
            <% dataList.forEach(function(item) { %>
                <li data-id="<%= item.id %>"><%= item.value %></li>
            <% }) %>
        </ul>
    <% } %>
</div>